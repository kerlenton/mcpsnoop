# Binaries
/mcpsnoop
/dist/
*.exe
*.out

# Traces / local artefacts
*.jsonl

# Go
/vendor/

# npm. Nothing in the repo creates this, but the npm directory invites an
# install in it.
node_modules/

# Editor / OS
.DS_Store
.idea/
.vscode/

# Local agent config (keep private)
.claude/settings.local.json
